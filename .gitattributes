# .bat 런처는 CP949(완성형 한글) + CRLF 로 저장되어 있습니다.
# git 의 줄바꿈/인코딩 변환으로 한글이 깨지지 않도록 변환을 끕니다(바이트 보존).
*.bat -text

# 한국어 텍스트 안내서도 인코딩 보존을 위해 변환 끔
*.txt -text

# PDF 는 바이너리
*.pdf binary

# 마크다운 문서는 LF 로 정규화
*.md text eol=lf
